<% provide(:title, 'Edit Book') %>
<h1>Edit Book</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@book) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Update Book", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
